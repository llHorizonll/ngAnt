import { AlertComponent } from './alert.component';

describe('AlertComponent', () => {
  it('should create an instance', () => {
    const directive = new AlertComponent();
    expect(directive).toBeTruthy();
  });
});
